name: Prepare and test Python

on:
  pull_request:
    branches: [main]
    paths:
      - '*.py'
      - 'requirements.txt'
      - '*.db'

jobs:
  build_python:
    uses: CorporalCiprian/todo-template/.github/workflows/build-and-test-python.yml@main
    with: 
      version: '3.12'

   


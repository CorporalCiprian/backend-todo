on:
  push:
    tags:
      - 'v*'

jobs:
  deploy_to_test:
    uses: CorporalCiprian/todo-template/.github/workflows/python/deploy-app.yml@main
    with:
      env: Test
      deploy: false
    

  